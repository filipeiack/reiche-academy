# =============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES - TEMPLATE
# =============================================================================
# This file is used with docker-compose.yml for local development
# Copy this file to .env and fill in the values
# DO NOT commit .env file to git (it's in .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# JWT AUTHENTICATION (LOCAL DEV)
# -----------------------------------------------------------------------------
# These are used by docker-compose.yml if not provided
# For local development, you can use simple values or leave defaults

# JWT access token secret (default will be used if not set)
JWT_SECRET=local-dev-jwt-secret-change-for-prod

# JWT access token expiration
JWT_ACCESS_EXPIRATION=2h

# JWT refresh token secret (default will be used if not set)
JWT_REFRESH_SECRET=local-dev-refresh-secret-change-for-prod

# JWT refresh token expiration
JWT_REFRESH_EXPIRATION=1d

# -----------------------------------------------------------------------------
# DATABASE & REDIS
# -----------------------------------------------------------------------------
# Note: For local development using docker-compose.yml,
# database and Redis are configured in the docker-compose.yml file
# and don't need to be set here.

# These variables are only needed if you're running the backend
# outside of Docker:

# DATABASE_URL=postgresql://reiche:reiche_dev_2024@localhost:5432/reiche_academy?schema=public
# REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------------------
# BUSINESS RULES (OPTIONAL)
# -----------------------------------------------------------------------------
# Auto-associate default pillars when creating company (true/false)
AUTO_ASSOCIAR_PILARES_PADRAO=true
